// window.alert("Good Day!");

// // Comment is done withh double forward slash
// // variables are usually denoted by the following keywords:
// // 1. var
// //2. const : is short for constant
// //3. let
// // Variables can also be thought as containers because they store values.
// var num = 200; // So num is storing 200
// //Data Types that can be stored in a variable:
// // Primitive data Types
// // 1. Number
// var number = 123;
// // 2. Strings
// var string = "123";
// var word = 'How are you'; // Single Quotes
// var word = "How are you"; // Double Quotes
// var word = `How are you`; //String Template literal
// // 3. Booleans
// var truth = true;
// var lie = false;
// // 4. Null
// var empty = null;
// var nothing = null;
// var brain = null;
// // 5. Undefined
// var aVariable; // Undefined !!Standard
// var variable = undefined;

// Working with Random Number and Math Objects
// console.log(Math.floor(Math.random() * 10));

// var userResponse = window.confirm("Should we proceed?");
// The window.confirm returns a boolean value.

// if (userResponse) {
//     console.log("User said Yes!");
// } else {
//     console.log("User clicked Cancel");
// }

//Work
// Create a random number between 0 and 100

// inbuilt functions in JavaScript
// eg. alert(), prompt(), confirm()
// Math Functions
// eg. Math.random() , Math.floor(), etc

// console.log( Math.floor( Math.random() * 10 ) +1);

// confirm("Click Something");

var userResponse = confirm("Are you OK? Click Ok if Yes? If No? Click Cancel.");

if( userResponse ) {
    console.log("User is ok!");
} else {
    console.log("User is ment!");
}

// Miles-to-kilometers converter

// Create a variable that contains a value in miles, convert it to kilometers, and log the value in kilometers in the following format:

// "The distance of 130 kms is equal to 209.2142 miles"

// For reference, 1 mile equals 1.60934 kilometers.

// BMI calculator
// Set values for height in inches and weight in pounds, then convert the values to centimeters and kilos, outputting the results to the console:
// • 1 inch is equal to 2.54 cm
// • 2.2046 pounds is equal to 1 kilo
// Output the results. Then, calculate and log the BMI: this is equal to weight (in kilos)
// divided by squared height (in meters). Output the results to the console.



